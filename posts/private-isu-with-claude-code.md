---
title: Claude Code に private-isu を3時間回させたら 873,466点だった
date: "2026-08-21"
tags: [ISUCON, Claude Code, パフォーマンス, Go]
summary: private-isu の計測から改善・デプロイ・記録まで全部 Claude Code に任せて、3時間ほど回してもらいました。4,140 → 873,466。
---

# Claude Code に private-isu を3時間回させたら 873,466点だった

ISUCON の練習に [private-isu](https://github.com/catatsuy/private-isu) を使って、**計測から改善・デプロイ・記録まで全部 Claude Code に回させたらどこまで行くのか**を試しました。

自分がやったのは環境の用意と、方針の判断（1台構成でレギュレーションを守る、ベンチ機を変える、など）だけで、コードは1行も書いていません。

- リポジトリ: [kyosu-1/private-isu-2026](https://github.com/kyosu-1/private-isu-2026)
- 環境構築に使った自作CLI: [kyosu-1/isuenv](https://github.com/kyosu-1/isuenv)

## 結果

**4,140 → 873,466（211倍）**、`fail = 0`、再起動試験も合格。

ベースラインを取ってから最高記録までは2時間32分（21:57 → 00:29）でした。

| 時刻 | スコア | 変更 |
|---|---:|---|
| 21:57 | 4,140 | ベースライン（Go / 無改善） |
| 22:15 | 36,013 | `comments` / `posts` にインデックス追加 |
| 22:20 | 38,061 | 一覧クエリを JOIN + LIMIT に（全件取得をやめる） |
| 22:22 | 39,072 | テンプレートを起動時に1回だけparse |
| 22:29 | 50,489 | 画像をファイル化して nginx 直配信 |
| 22:33 | 75,586 | `makePosts` の N+1 を IN句でまとめる |
| 22:35 | 84,487 | `digest()` の fork/exec を廃止 |
| 22:38 | 169,403 | 一覧クエリを `STRAIGHT_JOIN` で駆動表固定 |
| 22:43 | 185,773 | MySQLチューニング |
| 22:45 | 193,350 | nginxチューニング |
| 22:54 | 204,414 | `SELECT *` をやめて BLOB を読まない |
| 22:57 | 212,264 | `interpolateParams` + コネクションプール |
| 22:59 | 218,768 | 新規投稿の画像をDBに保存しない |
| | | **← ここでベンチマーカーを `c7a.xlarge` に変更（アプリは無変更）** |
| 23:24 | 286,936 | 同一コードで測り直し。差の67,416点は計測器の性能 |
| 23:31 | 318,452 | `users` のインメモリキャッシュ |
| 23:37 | 332,972 | 投稿のINSERTと画像作成をトランザクションで囲む |
| 23:49 | 340,404 | `GOGC=400` |
| 23:53 | **758,615** | **画像に `Cache-Control` / `expires`** |
| 23:59 | 767,638 | ユーザーキャッシュに `account_name` 索引 |
| 00:03 | 803,871 | テンプレートのreflect呼び出しを事前計算に |
| 00:29 | **873,466** | 計測ログOFF + 再起動後 |

途中でベンチマーカー側が先にCPU飽和していることが分かったので、そこで一度環境を作り直しています。同一コードで 219,520 → 286,936 になったので、この境目をまたいだスコアは比較できません。

## 環境

環境構築には自作の [isuenv](https://github.com/kyosu-1/isuenv) を使いました。ISUCON の過去問と private-isu の練習環境を、公開AMIから EC2 にコマンド一発で作って壊せる CLI です。

```sh
isuenv up private-isu --nodes 1 --bench --ttl 6h
```

private-isu のレギュレーションは **EC2 1台構成が前提**で、「起動したインスタンス以外の外部リソースを利用する行為（他のインスタンスに処理を委譲するなど）は禁止する」と明記されています。なので競技サーバーは `c7a.large`（2 vCPU / 3.7GB）1台のまま最後まで通しました。ベンチマーカーは競技対象外なので別インスタンスです。

## 進め方

最初にルールとして固定したのはこれだけです。

- **計測なき改善は禁止。** 変更前に必ず実測の根拠を示す
- **1改善 = 1コミット = 1ベンチ。** 混ぜない
- **スコアが下がったら即revert。** 粘らない
- **サーバー上で直接編集しない。** ローカルの git が正で、反映は `make deploy` だけ

そのうえで `make bench` を叩くと、ログ初期化 → ウォームアップ → ベンチ → alp / pt-query-digest / vmstat / pidstat の収集 → スコアログへの追記まで自動で走るようにしてもらいました。あとは延々これを回してもらうだけです。

最終的に計測64回・コミット50件。全部の改善にコミットメッセージと `measurements/` の実測根拠が残っています。

## 詳細

やったこと・効かなかったこと・踏んだ罠は全部リポジトリに残してあります。

- [docs/journal.md](https://github.com/kyosu-1/private-isu-2026/blob/main/docs/journal.md) — 実行コマンドとハマった点の記録
- [docs/playbook/](https://github.com/kyosu-1/private-isu-2026/tree/main/docs/playbook) — 本番で開く用の手順書
- [negative-results.md](https://github.com/kyosu-1/private-isu-2026/blob/main/docs/playbook/improvements/negative-results.md) — 効かなかったこと3件
- [scores/log.md](https://github.com/kyosu-1/private-isu-2026/blob/main/scores/log.md) — 全64回の計測記録

なお、ベンチを連続で回すと2回目以降が壊れる問題が未解決のまま残っています。おそらくベンチマーカー側のポート枯渇で、アプリではなく計測環境側の問題だと思っていますが、そこで時間切れになりました。

## おわりに

やらせてみた感想としては、**定石を知らなくても計測さえ回っていれば勝手に定石にたどり着く**な、というのが一番でした。インデックスも N+1 も静的ファイル配信も、こちらから一切示唆していません。alp とスロークエリを読ませたら順に潰していきました。

そうなると気になるのは、**今年の ISUCON はどうなるんでしょうね**、というところで。

このくらいのことが3時間で終わるなら、当日やるべきことは「速く書くこと」から「何を測るか決めること」と「エージェントを走らせる土台を先に作っておくこと」に寄っていく気がします。実際、今回いちばん効いたのも `make bench` を最初に整えたことでした。

とはいえ本番は初見の問題で、レギュレーションもマニュアルも当日読むわけで、そこがどう転ぶかは分かりません。出てみないと分からないので、まあ出ます。
